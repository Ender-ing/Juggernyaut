1[TXT $PATH]:
-couldn't fetch configurations from "$PATH"
2[TXT $MSG]:
-TOML parsing error: $MSG
3:
-any and all pre-existing import directories will be ignored (strict mode enabled)
4:
-invalid/inaccessible import directory (path ignored)
