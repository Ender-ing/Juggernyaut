/**
 * @brief
 * Handle CLI initialisation
**/

#pragma once

#include "common/headers.hpp"

namespace Console {
    namespace Internal {
        // Handle CLI initialisation
        extern void finalize(uint32_t activeSrcs) ;
    }
}
