/**
 * @brief
 * Handle CLI initialisation
**/

#pragma once

#include "../../core/common/headers.hpp"

namespace Console {
    namespace Internal {
        // Handle CLI initialisation
        extern void finalize(uint32_t activeSrcs) ;
    }
}
