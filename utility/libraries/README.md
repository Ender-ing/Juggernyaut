# Common functions

Common general-use functions and files are placed here!

> [!IMPORTANT]
> Make sure to keep all your variables and functions inside the `Common` namespace!

## Allowed components

All defined functions and variables must be **platform-agnostic**!

All kinds of headers, including *platform-specific (macros-guarded) headers*, are allowed.
