on:
  workflow_call: # Added this line to make it reusable
    inputs:
      windows_system:
        required: false
        type: string
        default: windows-2025-vs2026
      build_platform_name:
        required: true
        type: string
      build_mode:
        required: true
        type: string
      bundle_binaries:
        required: true
        type: boolean
      release_version:
        required: false
        type: string
        default: 0.0.0-github_windows.000
      run_tests:
        type: boolean
        default: true
      security_analysis:
        type: boolean
        default: false

jobs:

  # Windows
  build-binaries-on-windows:
    name: Windows ${{inputs.build_platform_name}}
    runs-on: ${{inputs.windows_system}}

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Initialize CodeQL
      if: inputs.security_analysis && !startsWith(inputs.build_platform_name, 'arm')
      uses: github/codeql-action/init@v4
      with:
        languages: 'c-cpp'

    - name: Setup CMake 4.2+
      uses: jwlawson/actions-setup-cmake@v2
      with:
        cmake-version: '4.2.x'

    - name: Set up JDK 23
      uses: actions/setup-java@v5
      with:
        java-version: '23'
        distribution: 'temurin'

    - name: Get MSVC tools arch
      run: |
        $arch = "${{inputs.build_platform_name}}"
        if ($arch -eq "x86_64") {
          echo "MSVC_ARCH=x64" >> $env:GITHUB_ENV
        } elseif ($arch -eq "x86_32") {
          echo "MSVC_ARCH=x86" >> $env:GITHUB_ENV
        } elseif ($arch -eq "arm64") {
          echo "MSVC_ARCH=arm64" >> $env:GITHUB_ENV
        } elseif ($arch -eq "arm32") {
          echo "MSVC_ARCH=arm" >> $env:GITHUB_ENV
        } else {
          Write-Error "Couldn't determine architecture version to use with MSVC!"
          exit 1
        }

    - name: Install Visual Studio Build Tools
      uses: ilammy/msvc-dev-cmd@v1.13.0
      with:
        arch: ${{env.MSVC_ARCH}}
        sdk: 10.0.26100.0

    - name: Install NSIS
      if: inputs.bundle_binaries
      run: choco install nsis -y

    - name: Install Qt Installer Framework (IFW)
      if: inputs.bundle_binaries && (inputs.build_platform_name == 'x86_64' || inputs.build_platform_name == 'arm64')
      uses: jurplel/install-qt-action@v4
      with:
        host: 'windows' # Use x64 bins
        no-qt-binaries: true
        # The syntax for tools is often <tool_name>,<version>,<architecture_identifier>
        tools: 'tools_ifw'
        set-env: false

    - name: Make the ".build" directory
      run: mkdir ${{github.workspace}}/.build

    - name: Configure CMake
      if: inputs.build_platform_name != 'x86_32'
      run: |
        cmake .. -DCMAKE_BUILD_TYPE=${{inputs.build_mode}} `
          -DCMAKE_TOOLCHAIN_FILE="../.cmake/target-configs/windows-${{inputs.build_platform_name}}-on-${{inputs.build_platform_name}}.cmake" `
          -DJUG_RELEASE_VERSION="${{inputs.release_version}}" -DJUG_BUILD_TESTS=${{inputs.run_tests}}
      working-directory: .build

    - name: Configure CMake (32-bit)
      if: inputs.build_platform_name == 'x86_32'
      run: |
        cmake .. -DCMAKE_BUILD_TYPE=${{inputs.build_mode}} `
          -DCMAKE_TOOLCHAIN_FILE="../.cmake/target-configs/windows-x86_32-on-x86_64.cmake" `
          -DJUG_RELEASE_VERSION="${{inputs.release_version}}" -DJUG_BUILD_TESTS=${{inputs.run_tests}}
      working-directory: .build

    - name: Build
      run: cmake --build . --config ${{inputs.build_mode}} --parallel 2 --verbose
      working-directory: .build

    - name: Run Tests (optional)
      if: inputs.run_tests
      run: ctest -C ${{inputs.build_mode}} -V -j 2
      working-directory: .build

    - name: Install (optional)
      if: inputs.bundle_binaries
      run: cmake --install . --config ${{inputs.build_mode}} --parallel 2 --verbose
      working-directory: .build

    - name: Package Native Installer (optional)
      if: inputs.bundle_binaries
      run: cpack --config ../.build/CPackConfig.cmake -C ${{inputs.build_mode}}
      working-directory: .dist

    - name: Extract version (optional)
      if: inputs.bundle_binaries
      run: |
        $version = (./jug-cmp --version).Trim()
        if ($version) {
          echo "VERSION=$version" >> $env:GITHUB_ENV
        } else {
          Write-Error "Couldn't get VERSION info from the generated binaries!"
          exit 1
        }
      working-directory: ${{github.workspace}}/.dist/${{inputs.build_mode}}/${{inputs.build_platform_name}}/bin/

    - name: Perform CodeQL Analysis
      if: inputs.security_analysis && !startsWith(inputs.build_platform_name, 'arm')
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:c-cpp"

    - name: Upload Binaries Artifact (optional)
      if: inputs.bundle_binaries
      uses: actions/upload-artifact@v7
      with:
        name: Juggernyaut-${{inputs.build_mode}}-v${{env.VERSION}}-Windows-${{inputs.build_platform_name}}
        path: ${{github.workspace}}/.dist/${{inputs.build_mode}}/${{inputs.build_platform_name}}/

    - name: Upload QT Installer Artifact (optional)
      if: inputs.bundle_binaries && (inputs.build_platform_name == 'x86_64' || inputs.build_platform_name == 'arm64')
      uses: actions/upload-artifact@v7
      with:
        name: Juggernyaut-Installer-Windows-${{inputs.build_platform_name}}
        path: ${{github.workspace}}/.dist/Juggernyaut-Installer-v${{env.VERSION}}-${{inputs.build_platform_name}}.exe

    - name: Upload NSIS Installer Artifact (optional)
      if: inputs.bundle_binaries
      uses: actions/upload-artifact@v7
      with:
        name: Juggernyaut-NSIS-Installer-Windows-${{inputs.build_platform_name}}
        path: ${{github.workspace}}/.dist/Juggernyaut-NSIS-Installer-v${{env.VERSION}}-${{inputs.build_platform_name}}.exe
