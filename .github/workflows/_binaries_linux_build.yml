on:
  workflow_call: # Added this line to make it reusable
    inputs:
      linux_system:
        required: false
        type: string
        default: ubuntu-latest
      build_platform_name:
        required: true
        type: string
      build_mode:
        required: true
        type: string
      bundle_binaries:
        required: true
        type: boolean
      release_version:
        required: false
        type: string
        default: 0.0.0-github_linux.000
      run_tests:
        type: boolean
        default: true
      security_analysis:
        type: boolean
        default: false

jobs:

  # Linux
  build-binaries-on-ubuntu:
    name: Linux ${{inputs.build_platform_name}}
    runs-on: ${{inputs.linux_system}}

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Initialize CodeQL
      if: inputs.security_analysis && !startsWith(inputs.build_platform_name, 'arm')
      uses: github/codeql-action/init@v4
      with:
        languages: 'c-cpp'

    - name: Setup CMake 4.2+
      uses: jwlawson/actions-setup-cmake@v2
      with:
        cmake-version: '4.2.x'

    - name: Set up JDK 23
      uses: actions/setup-java@v5
      with:
        java-version: '23'
        distribution: 'temurin'

    - name: Install dependencies
      run: |
        sudo apt-get update -o Acquire::Retries=3
        sudo apt-get install -y build-essential cmake valgrind

    - name: Install dependencies (64-bit)
      if: inputs.build_platform_name == 'x86_64' || inputs.build_platform_name == 'arm64'
      run: |
        # Install the native 64-bit libraries
        sudo apt-get install -y zlib1g-dev libpcre3-dev libssl-dev

    - name: Install dependencies (32-bit)
      if: inputs.build_platform_name == 'x86_32'
      run: |
        sudo dpkg --add-architecture i386
        sudo apt-get update -o Acquire::Retries=3
        sudo apt-get install -y gcc-multilib g++-multilib libstdc++6-13-dbg:i386 libgcc-s1:i386 zlib1g-dev:i386 libssl-dev:i386

    - name: Install dependencies (ARM 32-bit)
      if: inputs.build_platform_name == 'arm32'
      run: |
        sudo dpkg --add-architecture armhf
        sudo apt-get update -o Acquire::Retries=3
        sudo apt-get install -y gcc-arm-linux-gnueabihf g++-arm-linux-gnueabihf qemu-user-static zlib1g-dev:armhf libpcre3-dev:armhf libssl-dev:armhf

    - name: Install dependencies dependency (ARM64)
      if: inputs.build_platform_name == 'arm64'
      run: |
        sudo apt-get install -y libtiff-dev libwebp7
        sudo ln -s /usr/lib/aarch64-linux-gnu/libtiff.so.6 /usr/lib/aarch64-linux-gnu/libtiff.so.5
        sudo ln -s /usr/lib/aarch64-linux-gnu/libwebp.so.7 /usr/lib/aarch64-linux-gnu/libwebp.so.6

    - name: Install Qt Installer Framework (IFW)
      if: inputs.bundle_binaries && (inputs.build_platform_name == 'x86_64' || inputs.build_platform_name == 'arm64')
      uses: jurplel/install-qt-action@v4
      with:
        no-qt-binaries: true
        # The syntax for tools is often <tool_name>,<version>,<architecture_identifier>
        tools: 'tools_ifw'
        set-env: false

    - name: Make the ".build" directory
      run: mkdir ${{github.workspace}}/.build

    - name: Configure CMake
      if: inputs.build_platform_name != 'x86_32' && inputs.build_platform_name != 'arm32'
      run: |
        cmake .. -DCMAKE_BUILD_TYPE=${{inputs.build_mode}} \
          -DCMAKE_TOOLCHAIN_FILE="../.cmake/target-configs/linux-${{inputs.build_platform_name}}-on-${{inputs.build_platform_name}}.cmake" \
          -DJUG_RELEASE_VERSION="${{inputs.release_version}}" -DJUG_BUILD_TESTS=${{inputs.run_tests}}
      working-directory: .build

    - name: Configure CMake (32-bit)
      if: inputs.build_platform_name == 'x86_32'
      run: |
        cmake .. -DCMAKE_BUILD_TYPE=${{inputs.build_mode}} \
          -DCMAKE_TOOLCHAIN_FILE="../.cmake/target-configs/linux-x86_32-on-x86_64.cmake" \
          -DJUG_RELEASE_VERSION="${{inputs.release_version}}" -DJUG_BUILD_TESTS=${{inputs.run_tests}}
      working-directory: .build

    - name: Configure CMake (ARM 32-bit)
      if: inputs.build_platform_name == 'arm32'
      run: |
        cmake .. -DCMAKE_BUILD_TYPE=${{inputs.build_mode}} \
          -DCMAKE_TOOLCHAIN_FILE="../.cmake/target-configs/linux-arm32-on-arm64.cmake" \
          -DJUG_RELEASE_VERSION="${{inputs.release_version}}" -DJUG_BUILD_TESTS=${{inputs.run_tests}}
      working-directory: .build

    - name: Build
      run: cmake --build . --config ${{inputs.build_mode}} --parallel 2 --verbose
      working-directory: .build

    - name: Run Tests (optional)
      if: inputs.run_tests
      run: ctest -C ${{inputs.build_mode}} -V -j 2
      working-directory: .build

    - name: Install (optional)
      if: inputs.bundle_binaries
      run: cmake --install . --config ${{inputs.build_mode}} --parallel 2 --verbose
      working-directory: .build

    - name: Package Native Installer (optional)
      if: inputs.bundle_binaries
      run: cpack --config ../.build/CPackConfig.cmake -C ${{inputs.build_mode}}
      working-directory: .dist

    - name: Extract version (optional)
      if: inputs.bundle_binaries
      run: |
        set -e
        VERSION=$(${{github.workspace}}/.dist/${{inputs.build_mode}}/${{inputs.build_platform_name}}/bin/jug-cmp --version \
          | head -n 1)
        echo "VERSION=$VERSION" >> $GITHUB_ENV
    - name: Perform CodeQL Analysis
      if: inputs.security_analysis && !startsWith(inputs.build_platform_name, 'arm')
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:c-cpp"

    - name: Upload Binaries Artifact (optional)
      if: inputs.bundle_binaries
      uses: actions/upload-artifact@v7
      with:
        name: Juggernyaut-${{inputs.build_mode}}-v${{env.VERSION}}-Linux-${{inputs.build_platform_name}}
        path: ${{github.workspace}}/.dist/${{inputs.build_mode}}/${{inputs.build_platform_name}}/

    - name: Upload .run Installer Artifact (optional)
      if: inputs.bundle_binaries && (inputs.build_platform_name == 'x86_64' || inputs.build_platform_name == 'arm64')
      uses: actions/upload-artifact@v7
      with:
        name: Juggernyaut-Installer-Linux-${{inputs.build_platform_name}}
        path: ${{github.workspace}}/.dist/Juggernyaut-Installer-v${{env.VERSION}}-${{inputs.build_platform_name}}.run

    - name: Upload .deb Installer Artifact (optional)
      if: inputs.bundle_binaries
      uses: actions/upload-artifact@v7
      with:
        name: Juggernyaut-Installer-Linux-${{inputs.build_platform_name}}-native
        path: ${{github.workspace}}/.dist/Juggernyaut-Installer-v${{env.VERSION}}-${{inputs.build_platform_name}}.deb
