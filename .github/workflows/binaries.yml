name: Binaries
on:
  push:
    branches:
    - "main"
  pull_request:
    branches:
    - "main"

jobs:

  # Debug
  build-binaries-debug:
    name: Debug
    uses: ./.github/workflows/_binaries_dynamic_build.yml
    with:
      build_mode: Debug
      bundle_binaries: false

  # Release
  build-binaries-release:
    name: Release
    uses: ./.github/workflows/_binaries_dynamic_build.yml
    if: >
      (github.event_name == 'pull_request' && endsWith(github.event.pull_request.title, ' -FULL')) ||
      github.event_name == 'push'
    needs:
      - build-binaries-debug
    with:
      build_mode: Release
      bundle_binaries: false
